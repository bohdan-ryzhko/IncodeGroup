import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    position: 'relative',
  },
  container: {
    marginTop: 15,
  },
  buttonWrapper: {
    marginTop: 'auto',
  },
});
