import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginRight: 15,
    marginBottom: 15,
    flexDirection: 'row',
    justifyContent: 'flex-end',
  },
  button: {
    width: 'auto',
  },
});
